# Troubleshooting

If SOAP is unavailable, verify that the PHP SOAP extension is enabled.

If Oracle connectivity fails, verify the Oracle client, connection string and application credentials.

For OBI issues, validate the configured WSDL endpoint.
