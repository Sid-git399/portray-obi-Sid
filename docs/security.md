# Security

Security controls include prepared database statements, password hashing, CSRF protection, authenticated sessions and environment-based secrets.

Oracle BI credentials must never be exposed to the browser.
