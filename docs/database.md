# Database

The application database is separate from the OBI data warehouse.

The schema contains users, reports, categories, report parameters and history.

Initialize the schema before loading seed data.
