# API and Service Boundaries

Controllers handle application requests.

Models represent persistent data.

The OBI service isolates Oracle BI Publisher SOAP communication from the rest of the application.
