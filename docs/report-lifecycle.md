# Report Lifecycle

A report is selected from the catalogue, optional parameters are supplied, and the application requests the selected output format.

The generated document is then returned to the authorized user.
