# Deployment

Production deployment requires PHP with SOAP and Oracle connectivity.

Environment variables should be configured outside source control.

The application should run behind a properly configured web server.
