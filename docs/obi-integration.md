# Oracle BI Publisher

The OBI integration communicates with Oracle BI Publisher through SOAP.

Live mode uses the configured WSDL and credentials.

Demo mode serves pre-generated reports without requiring a live OBI server.
