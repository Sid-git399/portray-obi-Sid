# Coding Standards

Use strict typing for new PHP files.

Keep controllers focused on HTTP/application flow.

Keep database access inside models or database abstractions.

Keep Oracle BI Publisher communication inside the OBI service.
