# Project Structure

app contains the application logic.

config contains runtime configuration.

database contains Oracle SQL scripts.

public contains the web entry point and assets.

storage contains local demo report resources.
