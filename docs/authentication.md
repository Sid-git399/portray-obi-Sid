# Authentication

Authentication is handled by the application Auth component.

Passwords are verified using PHP password hashing APIs.

Authenticated sessions are regenerated during login and protected through middleware.
